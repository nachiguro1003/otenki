apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: otenki-app-ingress
spec:
  rules:
    - http:
        paths:
          - path: /*
            backend:
              serviceName: otenki-app
              servicePort: 80
